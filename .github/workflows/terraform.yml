name: Deploy DNS Records

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  deploy-dns:
    runs-on: self-hosted
    environment:
      name: DEV

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5

      - name: Debug env (WINDNS*)
        shell: bash
        run: |
          echo "WINDNS_SERVER from secrets: '${{ secrets.WINDNS_SERVER }}'"
          echo "WINDNS_USERNAME from secrets: '${{ secrets.WINDNS_USERNAME }}'"
          echo "WINDNS_PASSWORD is set: $([[ -n "${{ secrets.WINDNS_PASSWORD }}" ]] && echo yes || echo no)"

      - name: Terraform Init
        shell: bash
        working-directory: ./org/_account_template
        run: |
          export WINDNS_SERVER="${{ secrets.WINDNS_SERVER }}"
          export WINDNS_USERNAME="${{ secrets.WINDNS_USERNAME }}"
          export WINDNS_PASSWORD="${{ secrets.WINDNS_PASSWORD }}"
          export WINDNS_DNS_SERVER_HOSTNAME="${{ secrets.WINDNS_DNS_SERVER_HOSTNAME }}"
          terraform init -reconfigure

      - name: Terraform Plan
        shell: bash
        working-directory: ./org/_account_template
        run: |
          export WINDNS_SERVER="${{ secrets.WINDNS_SERVER }}"
          export WINDNS_USERNAME="${{ secrets.WINDNS_USERNAME }}"
          export WINDNS_PASSWORD="${{ secrets.WINDNS_PASSWORD }}"
          export WINDNS_DNS_SERVER_HOSTNAME="${{ secrets.WINDNS_DNS_SERVER_HOSTNAME }}"
          terraform plan -var-file="../westside/dns.tfvars"

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main'
        shell: bash
        working-directory: ./org/_account_template
        run: |
          export WINDNS_SERVER="${{ secrets.WINDNS_SERVER }}"
          export WINDNS_USERNAME="${{ secrets.WINDNS_USERNAME }}"
          export WINDNS_PASSWORD="${{ secrets.WINDNS_PASSWORD }}"
          export WINDNS_DNS_SERVER_HOSTNAME="${{ secrets.WINDNS_DNS_SERVER_HOSTNAME }}"
          terraform apply -auto-approve -var-file="../westside/dns.tfvars"